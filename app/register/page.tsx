import { RegisterPage } from "@/components/auth/register-page";

export const metadata = {
  title: "Register - BloodBank",
  description: "Register as a donor to start saving lives",
};

export default function Register() {
  return <RegisterPage />;
}
